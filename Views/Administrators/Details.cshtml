@model EventPlatFormVer4.Models.Administrator

@{
    ViewData["Title"] = "Details";
}

<h2>首页</h2>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Email)
        </dd>
    </dl>
</div>
<div>
    <button><a asp-action="Info" asp-route-id="@Model.Id">工作台</a></button>
    <button><a asp-action="Edit" asp-route-id="@Model.Id">修改信息</a> </button>
    <button><a asp-action="Delete" asp-route-id="@Model.Id">注销</a></button>
</div>
